apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "age-calci.fullname" . }}-test-connection"
  labels:
    {{- include "age-calci.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "age-calci.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
