apiVersion: v1
kind: Service
metadata:
  name: {{ include "age-calci.fullname" . }}
  labels:
    {{- include "age-calci.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "age-calci.selectorLabels" . | nindent 4 }}
